{
	auto_https off
	admin off
}

:{$PORT:8080} {
	root * /srv
	encode gzip

	handle /health {
		respond "ok" 200
	}

	handle /bff* {
		reverse_proxy {$API_UPSTREAM}
	}

	handle /users* {
		reverse_proxy {$API_UPSTREAM}
	}

	handle {
		try_files {path} /index.html
		file_server
	}
}
