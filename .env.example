# Variáveis de ambiente do frontend.
# Copie para `.env.local` só se precisar de overrides de Vite.
# Login OIDC passa pelo BFF (`/bff/*`); tokens não entram no bundle.

# Runtime do Caddy na imagem de prod (não é VITE_):
# API_UPSTREAM=http://domus-back.railway.internal:8080
